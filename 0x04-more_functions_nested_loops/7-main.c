#include "main.h"
#include <stdio.h>

/**
 * print_diagonal - prints a diagonal 
 * @n: length of the line
 * Return: returns nothing
 */

void print_diagonal(int n)
{
	i = 1;

	if (n > 0)
	{
		putchar(92)

		while (i < n)
		{
			count = 0;

			putchar('\n')
			{
				putchar(' ')
					count++;
			}
			putchar(92)
				i++;
		}
	}
	putchar('\n')
}
