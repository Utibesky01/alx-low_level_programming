#include "main.h"

/**
 * _isdigit - function that checks for uppercase character
 * @c: int type number
 *Return: 1 if c is uppercase, 0 otherwise
 */
int _isdigit(int c)
{
	if ((c >= 48) && (c <= 57))
		return (i);
	else
		return (0);
}
